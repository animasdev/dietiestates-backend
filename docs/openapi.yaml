openapi: 3.0.3
info:
  title: DietiEstates25 API
  version: 0.1.0
  description: |
    REST API for DietiEstates25.
servers:
  - url: https://api.example.com
    description: Production (placeholder)
  - url: http://localhost:8080
    description: Local dev (placeholder)
tags:
  - name: Auth
  - name: Listings
  - name: Listing Photos
  - name: Features
  - name: Users
  - name: Agencies
  - name: Agents
  - name: Media
  - name: Moderation
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
  parameters:
    Page:
      name: page
      in: query
      description: 0-based page index
      required: false
      schema: { type: integer, minimum: 0, default: 0 }
    Size:
      name: size
      in: query
      description: Page size (items per page)
      required: false
      schema: { type: integer, minimum: 1, maximum: 200, default: 20 }
    Sort:
      name: sort
      in: query
      description: Sort expression, e.g. 'createdAt,desc' or 'priceCents,asc'
      required: false
      schema: { type: string }
  schemas:
    Problem:
      type: object
      description: RFC 7807 Problem Details
      properties:
        type: { type: string, format: uri }
        title: { type: string }
        status: { type: integer }
        detail: { type: string }
        instance: { type: string, format: uri }
      additionalProperties: true
    Page:
      type: object
      properties:
        page: { type: integer, example: 0 }
        size: { type: integer, example: 20 }
        total: { type: integer, example: 123 }
        items:
          type: array
          items: { $ref: '#/components/schemas/Listing' }
    Listing:
      type: object
      required: [id, title, listingType, status, city, geo, createdAt]
      properties:
        id: { type: string, format: uuid }
        agencyId: { type: string, format: uuid }
        ownerAgentId: { type: string, format: uuid }
        listingType:
          type: string
          description: Vendita/Affitto
          enum: [SALE, RENT]
        status:
          type: string
          enum: [DRAFT, PUBLISHED, PENDING_DELETE, DELETED]
        title: { type: string }
        description: { type: string }
        priceCents: { type: integer, nullable: true }
        city: { type: string }
        address: { type: string, nullable: true }
        postalCode: { type: string, nullable: true }
        rooms: { type: integer, nullable: true }
        sizeSqm: { type: number, format: float, nullable: true }
        floor: { type: integer, nullable: true }
        energyClass:
          type: string
          nullable: true
          enum: [A4, A3, A2, A1, B, C, D, E, F, G]
        contractDescription: { type: string, nullable: true }
        securityDepositCents: { type: integer, format: int64, nullable: true }
        furnished: { type: boolean, nullable: true }
        condoFeeCents: { type: integer, format: int64, nullable: true }
        petsAllowed: { type: boolean, nullable: true }
        features:
          type: array
          items: { type: string }
        geo:
          type: object
          properties:
            lat: { type: number, format: float }
            lng: { type: number, format: float }
        photos:
          type: array
          items: { $ref: '#/components/schemas/ListingPhoto' }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    ListingCreate:
      type: object
      required: [title, listingType, description, energyClass]
      properties:
        title: { type: string }
        description: { type: string }
        listingType: { type: string, enum: [SALE, RENT] }
        isPublished: {type: boolean, default: false }
        priceCents: { type: integer, nullable: true }
        city: { type: string }
        address: { type: string }
        postalCode: { type: string }
        rooms: { type: integer }
        sizeSqm: { type: number, format: float }
        floor: { type: integer }
        energyClass:
          type: string
          enum: [A4, A3, A2, A1, B, C, D, E, F, G]
        contractDescription: { type: string }
        securityDepositCents: { type: integer, format: int64 }
        furnished: { type: boolean, default: false }
        condoFeeCents: { type: integer, format: int64 }
        petsAllowed: { type: boolean, default: false }
        features:
          type: array
          items: { type: string }
        geo:
          type: object
          required: [lat, lng]
          properties:
            lat: { type: number, format: float }
            lng: { type: number, format: float }
    ListingUpdate:
      type: object
      description: Fields are optional; send only those to update
      properties:
        title: { type: string }
        description: { type: string }
        priceCents: { type: integer, nullable: true }
        sizeSqm: { type: number, format: float }
        rooms: { type: integer }
        floor: { type: integer }
        energyClass:
          type: string
          enum: [A4, A3, A2, A1, B, C, D, E, F, G]
        contractDescription: { type: string }
        securityDepositCents: { type: integer, format: int64 }
        furnished: { type: boolean }
        condoFeeCents: { type: integer, format: int64 }
        petsAllowed: { type: boolean }
        address: { type: string }
        city: { type: string }
        postalCode: { type: string }
        features:
          type: array
          items: { type: string }
        geo:
          type: object
          properties:
            lat: { type: number, format: float }
            lng: { type: number, format: float }
    ListingPhoto:
      type: object
      properties:
        id: { type: string, format: uuid }
        url: { type: string, format: uri }
        position: { type: integer }
    DeleteRequest:
      type: object
      properties:
        reason:
          type: string
          maxLength: 500
          description: Motivo della cancellazione. Obbligatorio per amministratori, opzionale per gli altri ruoli.
    ModerationAction:
      type: object
      required: [id, listingId, performedByUserId, performedByRole, actionType, createdAt]
      properties:
        id: { type: string, format: uuid }
        listingId: { type: string, format: uuid }
        performedByUserId: { type: string, format: uuid }
        performedByRole:
          type: string
          enum: [SUPERADMIN, ADMIN, AGENCY, AGENT, USER]
        actionType:
          type: string
          enum: [EDIT, DELETE, RESTORE]
        reason:
          type: string
          maxLength: 500
          nullable: true
        createdAt: { type: string, format: date-time }
    UserInfo:
      type: object
      required: [displayName, email, role]
      properties:
        displayName: {type: string}
        email: {type: string, format: email}
        role: {type: string}
        agencyProfile:
          $ref: '#/components/schemas/UserInfoAgencyProfile'
        agentProfile:
          $ref: '#/components/schemas/UserInfoAgentProfile'
    UserInfoAgencyProfile:
      type: object
      properties:
        name: { type: string }
        description: { type: string }
        logoUrl:
          type: string
          format: uri
          nullable: true
          description: URL pubblico del logo agenzia
    UserInfoAgentProfile:
      type: object
      properties:
        agencyId: { type: string, format: uuid }
        reaNumber: { type: string }
        profilePhotoUrl:
          type: string
          format: uri
          nullable: true
          description: URL pubblico della foto profilo agente
    AdminSummary:
      type: object
      required: [id, displayName, email, role, firstAccess]
      properties:
        id: { type: string, format: uuid }
        displayName: { type: string }
        email: { type: string, format: email }
        role: { type: string }
        firstAccess: { type: boolean }
        createdAt: { type: string, format: date-time, nullable: true }
        updatedAt: { type: string, format: date-time, nullable: true }
    AgencySummary:
      type: object
      required: [id, name, description, ownerUserId, ownerDisplayName, ownerEmail]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        ownerUserId: { type: string, format: uuid }
        ownerDisplayName: { type: string }
        ownerEmail: { type: string, format: email }
        logoUrl: { type: string, format: uri, nullable: true }
        approvedBy: { type: string, format: uuid, nullable: true }
        approvedAt: { type: string, format: date-time, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AgentSummary:
      type: object
      required: [id, userId, displayName, email, agencyId, agencyName, reaNumber]
      properties:
        id: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        displayName: { type: string }
        email: { type: string, format: email }
        agencyId: { type: string, format: uuid }
        agencyName: { type: string }
        reaNumber: { type: string }
        profilePhotoUrl: { type: string, format: uri, nullable: true }
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    PageMetadata:
      type: object
      required: [page, size, totalElements, totalPages]
      properties:
        page: { type: integer, minimum: 0 }
        size: { type: integer, minimum: 1 }
        totalElements: { type: integer, minimum: 0 }
        totalPages: { type: integer, minimum: 0 }
        sort:
          type: array
          items: { type: string }
          description: Lista dei criteri di ordinamento applicati (`campo,DIREZIONE`).
    AdminSummaryPage:
      type: object
      required: [items, page]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AdminSummary' }
        page: { $ref: '#/components/schemas/PageMetadata' }
    AgencySummaryPage:
      type: object
      required: [items, page]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AgencySummary' }
        page: { $ref: '#/components/schemas/PageMetadata' }
    AgentSummaryPage:
      type: object
      required: [items, page]
      properties:
        items:
          type: array
          items: { $ref: '#/components/schemas/AgentSummary' }
        page: { $ref: '#/components/schemas/PageMetadata' }
    AgencyCreateRequest:
      type: object
      required: [name, description]
      properties:
        name:
          type: string
          minLength: 1
          description: Nome legale o commerciale dell'agenzia
        description:
          type: string
          minLength: 1
          description: Descrizione breve dell'agenzia
        logoMediaId:
          type: string
          format: uuid
          nullable: true
          description: Identificativo dell'asset già caricato tramite media API
    Agency:
      type: object
      required: [id, name, description, userId, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        description: { type: string }
        userId: { type: string, format: uuid }
        logoMediaId:
          type: string
          format: uuid
          nullable: true
        approvedBy:
          type: string
          format: uuid
          nullable: true
        approvedAt:
          type: string
          format: date-time
          nullable: true
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    AgentCreateRequest:
      type: object
      required: [agencyId, reaNumber]
      properties:
        agencyId:
          type: string
          format: uuid
          description: Agenzia a cui l'agente è associato
        reaNumber:
          type: string
          minLength: 1
          description: Numero REA dell'agente
        profilePhotoMediaId:
          type: string
          format: uuid
          nullable: true
          description: Identificativo dell'asset foto profilo già caricato
    Agent:
      type: object
      required: [id, userId, agencyId, reaNumber, createdAt, updatedAt]
      properties:
        id: { type: string, format: uuid }
        userId: { type: string, format: uuid }
        agencyId: { type: string, format: uuid }
        reaNumber: { type: string }
        profilePhotoMediaId:
          type: string
          format: uuid
          nullable: true
        createdAt: { type: string, format: date-time }
        updatedAt: { type: string, format: date-time }
    MediaUploadResponse:
      type: object
      required: [assetId, publicUrl]
      properties:
        assetId: { type: string, format: uuid }
        publicUrl: { type: string, format: uri }
        mimeType: { type: string }
        widthPx: { type: integer, nullable: true }
        heightPx: { type: integer, nullable: true }
      description: Risposta con i metadati principali dell'asset memorizzato.
paths:
  /auth/login:
    post:
      tags: [Auth]
      summary: Login with email/password
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email: { type: string, format: email }
                password: { type: string, format: password }
      responses:
        '200':
          description: Authenticated
          content:
            application/json:
              schema:
                type: object
                properties:
                  accessToken: { type: string }
                  refreshToken: { type: string }
                  firstAccess:
                    type: boolean
                    description: true se l'utente è al primo accesso e deve cambiare password
                  invitedBy:
                    type: string
                    format: uuid
                    nullable: true
                    description: |
                      Identificativo dell'utente che ha generato l'invito; valorizzato solo se `firstAccess` è true
        '401':
          description: Invalid credentials
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
  /auth/refresh:
    post:
      tags: [Auth]
      summary: Refresh access token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refreshToken]
              properties:
                refreshToken: { type: string }
      responses:
        '200':
          description: New access token
          content:
            application/json:
              schema:
                type: object
                properties:
                  accessToken: { type: string }
                  refreshToken: { type: string }
        '401':
          description: Invalid refresh token
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
  /auth/password-reset/request:
    post:
      tags: [Auth]
      summary: Request password reset email
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email: { type: string, format: email }
      responses:
        '202': { description: Reset initiated if email exists }
  /auth/password-reset/confirm:
    post:
      tags: [Auth]
      summary: Confirm password reset with token
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, newPassword]
              properties:
                token: { type: string }
                newPassword: { type: string, format: password, minLength: 8 }
      responses:
        '200': { description: Password updated }
        '400':
          description: Invalid or expired token
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /auth/sign-up/request:
    post:
      tags: [Auth]
      summary: Start email sign-up flow
      description: |
        Initiates user registration by creating a confirmation token and sending a confirmation email.
        Always responds 202 to avoid user enumeration. If a valid token already exists for the email,
        keeps the same token value and refreshes its expiration.
        If called with a valid SUPERADMIN bearer token, an optional `roleCode` can be provided to invite
        a user with a specific role (must match an existing role code). Otherwise, the role defaults to `USER`.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, displayName]
              properties:
                email: { type: string, format: email }
                displayName: { type: string, minLength: 1 }
                roleCode:
                  type: string
                  description: Optional, honored only for SUPERADMIN callers; must match an existing role code
      responses:
        '202': { description: Sign-up initiated if eligible }

  /auth/sign-up/confirm:
    post:
      tags: [Auth]
      summary: Confirm sign-up with token
      description: Completes registration by validating the token and setting the initial password.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [token, password]
              properties:
                token: { type: string }
                password: { type: string, format: password, minLength: 8 }
      responses:
        '200':
          description: Registration completed
          content:
            application/json:
              schema:
                type: object
                required: [status]
                properties:
                  status:
                    type: string
                    description: Confirmation outcome
                    example: confirmed
        '400':
          description: Invalid or expired token
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /auth/password:
    put:
      tags: [ Auth ]
      summary: Change passwod
      description: Changes the current password for the authenticated user.
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [ oldPassword, newPassword ]
              properties:
                oldPassword: { type: string, format: password, minLength: 8 }
                newPassword: { type: string, format: password, minLength: 8 }
      responses:
        '200': { description: password changed }
        '400':
          description: Invalid or expired token
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /listings:
    get:
      tags: [Listings]
      summary: Search listings (supports geo filters)
      description: |
        Accepts filters for type, price, city, features, and optional geospatial radius (lat,lng,radiusMeters).
        Implements pagination and sorting. Matches R3 and 2-design search notes.
      security:
        - {}
      parameters:
        - name: type
          in: query
          schema: { type: string, enum: [SALE, RENT] }
        - name: city
          in: query
          schema: { type: string }
        - name: minPrice
          in: query
          schema: { type: integer, minimum: 0 }
        - name: maxPrice
          in: query
          schema: { type: integer, minimum: 0 }
        - name: rooms
          in: query
          schema: { type: integer, minimum: 0 }
        - name: energyClass
          in: query
          schema: { type: string }
        - name: features
          in: query
          schema:
            type: array
            items: { type: string }
          style: form
          explode: true
        - name: status
          in: query
          schema: { type: string, enum: [PUBLISHED, DRAFT, PENDING_DELETE] }
          description: Public clients should only receive PUBLISHED; others require auth/role
        - name: lat
          in: query
          schema: { type: number, format: float }
        - name: lng
          in: query
          schema: { type: number, format: float }
        - name: radiusMeters
          in: query
          schema: { type: integer, minimum: 50, maximum: 100000 }
        - name: hasPhotos
          in: query
          schema: { type: boolean }
          description: |
            Filtra gli annunci in base alla presenza di foto.
            - `true`: restituisce solo annunci con almeno una foto.
            - `false`: restituisce solo annunci senza foto.
        - name: agencyId
          in: query
          schema: { type: string, format: uuid }
          description: Filtra gli annunci per agenzia proprietaria.
        - name: ownerAgentId
          in: query
          schema: { type: string, format: uuid }
          description: Filtra gli annunci per agente proprietario.
        - $ref: '#/components/parameters/Page'
        - $ref: '#/components/parameters/Size'
        - $ref: '#/components/parameters/Sort'
      responses:
        '200':
          description: Paginated listings
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Page' }
    post:
      tags: [Listings]
      summary: Create listing (published or draft)
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ListingCreate' }
      responses:
        '201':
          description: Created
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Listing' }
        '400':
          description: Validation error
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /listings/{id}:
    get:
      tags: [Listings]
      summary: Get listing by id
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Listing
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Listing' }
        '404': { description: Not found }
    patch:
      tags: [Listings]
      summary: Update listing fields
      description: Server enforces role-based field restrictions for admin edits.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/ListingUpdate' }
      responses:
        '200':
          description: Updated listing
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Listing' }
        '400':
          description: Validation error
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '403':
          description: Forbidden field updates for role
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /listings/{id}/delete:
    post:
      tags: [Listings]
      summary: Request deletion (soft) → PENDING_DELETE within 24h window
      description: Implements RD2/RD3. Admin must provide reason; agents can mark their own listings.
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/DeleteRequest' }
      responses:
        '200':
          description: Listing state updated to PENDING_DELETE
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Listing' }

  /listings/{id}/restore:
    post:
      tags: [Listings]
      summary: Restore a listing from PENDING_DELETE back to PUBLISHED
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Listing restored
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Listing' }
        '409':
          description: Window elapsed; cannot restore
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /moderation/listings:
    get:
      tags: [Moderation]
      summary: Elenco azioni di moderazione sugli annunci
      description: Restituisce le azioni di moderazione registrate per gli annunci accessibili all'utente autenticato.
      security:
        - bearerAuth: []
      parameters:
        - name: listingId
          in: query
          required: false
          description: Filtra le azioni per uno specifico annuncio (deve essere accessibile all'utente).
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Azioni di moderazione trovate
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/ModerationAction' }
        '401':
          description: Autenticazione richiesta
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '403':
          description: Accesso negato se l'utente non è autorizzato per l'annuncio richiesto
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /listings/{id}/photos:
    get:
      tags: [Listing Photos]
      summary: List photos for a listing
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '200':
          description: Photos
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/ListingPhoto' }
    post:
      tags: [Listing Photos]
      summary: Upload and attach photos (multipart)
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [files, positions]
              properties:
                files:
                  type: array
                  items:
                    type: string
                    format: binary
                positions:
                  type: array
                  items: { type: integer }
            encoding:
              files:
                style: form
      responses:
        '201':
          description: Photos added
          content:
            application/json:
              schema:
                type: array
                items: { $ref: '#/components/schemas/ListingPhoto' }

  /listings/{id}/photos/{photoId}:
    delete:
      tags: [Listing Photos]
      summary: Remove a photo from listing
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema: { type: string, format: uuid }
        - name: photoId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        '204': { description: Removed }

  /features:
    get:
      tags: [Features]
      summary: List available features (read-only lookup)
      responses:
        '200':
          description: Feature codes
          content:
            application/json:
              schema:
                type: array
                items: { type: string }

  /users/me:
    get:
      tags: [Users]
      summary: Return the logged-in user information.
      security:
        - bearerAuth: []
      responses:
        '200':
          description: User information
          content:
            application/json:
              schema: { $ref: '#/components/schemas/UserInfo' }
  /users/admins:
    get:
      tags: [Users]
      summary: Elenca tutti gli account amministrativi
      description: Restituisce gli utenti con ruolo `SUPERADMIN` o `ADMIN`, includendo gli stati di first access.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
          description: Numero pagina (0-based).
        - name: size
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
          description: Numero di elementi per pagina.
        - name: sort
          in: query
          schema:
            type: array
            items: { type: string }
          style: form
          explode: true
          description: Criteri di ordinamento (`displayName,asc`, `email,desc`, `createdAt,desc`).
        - name: search
          in: query
          schema: { type: string }
          description: Filtro su nome visualizzato o email (case-insensitive).
        - name: role
          in: query
          schema:
            type: string
            enum: [SUPERADMIN, ADMIN]
          description: Limita il risultato a un singolo ruolo amministrativo.
      responses:
        '200':
          description: Elenco di utenti amministratori
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AdminSummaryPage' }
        '403':
          description: Ruolo non autorizzato alla consultazione
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /users/agencies:
    get:
      tags: [Users]
      summary: Elenca tutte le agenzie censite
      description: Accessibile ad `ADMIN` e `SUPERADMIN`, riporta i dati principali dell'agenzia e del referente.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: size
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - name: sort
          in: query
          schema:
            type: array
            items: { type: string }
          style: form
          explode: true
          description: Criteri di ordinamento (`name,asc`, `createdAt,desc`, …).
        - name: search
          in: query
          schema: { type: string }
          description: Filtro su nome agenzia o referente.
        - name: approved
          in: query
          schema: { type: boolean }
          description: Se impostato, filtra in base allo stato di approvazione (`approvedAt` valorizzato).
      responses:
        '200':
          description: Elenco agenzie
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgencySummaryPage' }
        '403':
          description: Ruolo non autorizzato alla consultazione
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /users/agents:
    get:
      tags: [Users]
      summary: Elenca i profili agente disponibili
      description: |
        - `SUPERADMIN` e `ADMIN` vedono tutti gli agenti registrati.
        - `AGENCY` visualizza solo gli agenti appartenenti alla propria agenzia.
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema: { type: integer, minimum: 0, default: 0 }
        - name: size
          in: query
          schema: { type: integer, minimum: 1, maximum: 100, default: 20 }
        - name: sort
          in: query
          schema:
            type: array
            items: { type: string }
          style: form
          explode: true
          description: Criteri di ordinamento (`displayName,asc`, `reaNumber,desc`, `createdAt,desc`).
        - name: search
          in: query
          schema: { type: string }
          description: Ricerca su nome, email o numero REA.
        - name: agencyId
          in: query
          schema: { type: string, format: uuid }
          description: Filtra per agenzia (facoltativo per admin/superadmin, ignorato per utenti agency).
      responses:
        '200':
          description: Elenco agenti
          content:
            application/json:
              schema: { $ref: '#/components/schemas/AgentSummaryPage' }
        '403':
          description: Ruolo non autorizzato alla consultazione
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /agencies:
    post:
      tags: [Agencies]
      summary: Completa il profilo dell'agenzia al primo accesso
      description: |
        Utilizzato dagli utenti con ruolo `AGENCY` dopo la prima autenticazione per censire i
        dettagli dell'agenzia. L'ID proprietario viene derivato dal token (`sub`) e il campo
        `firstAccess` dell'utente viene impostato a `false` al termine del flusso.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AgencyCreateRequest' }
      responses:
        '201':
          description: Profilo agenzia creato
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Agency' }
        '400':
          description: Dati non validi
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '401':
          description: Richiesta non autenticata
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '403':
          description: Ruolo non autorizzato a completare il profilo agenzia
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '409':
          description: Profilo già completato o first access già consumato
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /agents:
    post:
      tags: [Agents]
      summary: Completa il profilo dell'agente al primo accesso
      description: |
        Utilizzato dagli utenti con ruolo `AGENT` dopo la prima autenticazione per associare il
        proprio profilo a un'agenzia e fornire il numero REA. L'ID utente è ricavato dal token e
        `firstAccess` viene impostato a `false` quando l'operazione va a buon fine.
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: '#/components/schemas/AgentCreateRequest' }
      responses:
        '201':
          description: Profilo agente creato
          content:
            application/json:
              schema: { $ref: '#/components/schemas/Agent' }
        '400':
          description: Dati non validi o agenzia inesistente
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '401':
          description: Richiesta non autenticata
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '403':
          description: Ruolo non autorizzato a completare il profilo agente
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '409':
          description: Profilo già completato o first access già consumato
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }

  /media/uploads:
    post:
      tags: [Media]
      summary: Carica un asset multimediale e restituisce i riferimenti
      description: |
        Endpoint destinato ad upload di loghi agenzia e foto profilo agente.
        Supporta file `image/jpeg`, `image/png`, `image/webp` (max 5MB). Il file viene salvato su filesystem locale
        (`storage/media/<categoria>/...`) e reso disponibile tramite `publicUrl`.
        Necessita autenticazione; l'asset viene attribuito all'utente proprietario (`createdBy`).
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [categoryCode, file]
              properties:
                categoryCode:
                  type: string
                  description: Codice categoria asset (es. `AGENCY_LOGO`, `AGENT_AVATAR`).
                file:
                  type: string
                  format: binary
                  description: File immagine da caricare.
      responses:
        '201':
          description: Asset caricato con successo
          content:
            application/json:
              schema: { $ref: '#/components/schemas/MediaUploadResponse' }
        '400':
          description: File non valido o categoria sconosciuta
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
        '401':
          description: Richiesta non autenticata
          content:
            application/problem+json:
              schema: { $ref: '#/components/schemas/Problem' }
